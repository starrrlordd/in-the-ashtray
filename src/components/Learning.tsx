let isActive: boolean = false;

let hasPermission = false;

let decimal: number = 0;

let binary: number = 0b1010;

let float = 3.14;

type aboutMe = {
    fullname: string
    age: number
    color: string
    sentence: string

}

const Learning: aboutMe ({aboutMe}) => {
    return <div>
    </div>
}

let color: string = "pink";
let fullName: string = "John Black";
let age: number = 12;
let sentence: string = `Hello, my name is ${fullName}, I am ${age} years old,
and my favorite color is ${color}`;

console.log(sentence);

let greeting: string = "Hello world";

let userCount: number = 3322;

let isLoading: boolean = false;

let totalScores: number[] = [32, 38, 98];

function greet(name: string): string {
    return `Hello ${name}`;

    
}
greet("Alice");

function greetingMorning(name: string): string {
    return `Hello ${name}`;    
}

greetingMorning("Alice");

let username = "Alice";

let flags = [true, false, true];

function add(a: number, b: number) {
    return a + b;
}

console.log(add)

const user = {
    name: "Alice",
    age: 30,
    isAdmin: true,
}

console.log(user.name)
console.log(user.isAdmin)
console.log(user.email)

const names: string[] = [];
console.log(names.push("Dylan"))





























export default Learning;